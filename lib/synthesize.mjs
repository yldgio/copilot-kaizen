// lib/synthesize.mjs — Synthesis engine stub (Phase 4)
// Will be fully implemented in Phase 4.

/**
 * Run the synthesis pipeline: condense kaizen entries into an AGENTS.md
 * context block and write it to the project's .kaizen directory.
 * Phase 4 stub: no-op.
 *
 * @param {{ db: import('better-sqlite3').Database, projectPath: string, kaizenDir: string, globalKaizenDir: string }} _opts
 */
export function synthesize(_opts) {
  // no-op until Phase 4
}
